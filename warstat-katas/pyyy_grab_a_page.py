# enter an URL
# save the html code to a db
# scan and get the embedded css and js aso.
# create a python-template from it to start a dynamic page
#    template should consist of classes for ALL (whitelisted) html elements on the page, like "page, headers, links, tables, divisions..."
#
# use this to instantly generate a login for (phishing demo) or a feedback form (sample use case) 